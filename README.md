# SDEWay / VG

Personal learning notes and portfolio site (Astro + Starlight), deployed on Cloudflare Pages.

## Develop

```bash
bun install
bun run dev
```

## Build

```bash
bun run build
```

Content lives in `src/content/docs/`. Admin publishing UI is at `/admin` (Cloudflare Pages Functions + GitHub).
